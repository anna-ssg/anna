{{ define "posts"}}
{{ template "head" .}}

<body>
    {{template "header" .}}
    <div class="body">
        <article>
            <section class="posts">
                {{range $PostDate, $Post := .Posts}}
                <a class="post-card" href="posts/{{$Post.FilenameWithoutExtension}}.html">
                    <div class="post-card-div">
                        <h3>{{$Post.Frontmatter.Title}}</h3>
                        <p>{{$Post.Frontmatter.Description}}</p>
                        <p>{{$Post.Frontmatter.Date}}</p>
                    </div>
                </a>
                {{end}}
            </section>
        </article>
    </div>

    {{template "footer" .}}
</body>

</html>

{{ end}}